---
platform: linux

inputs:
- name: accounts-git

outputs:
- name: circleci-output

params:
  CIRCLE_API_TOKEN: ((os-auto-circleci-api-token))
  CIRCLE_BRANCH: ((os-auto-circleci-branch))
  STUDENT_PASSWORD: ((accounts-autoqa-student-password))
  TEACHER_PASSWORD: ((accounts-autoqa-teacher-password))
  ADMIN_PASSWORD: ((accounts-autoqa-admin-password))

run:
  path: bash
  args:
    - -exc
    - |
      python3 accounts-git/concourse/test-release-candidate-environment/test-release-candidate-environment.py
