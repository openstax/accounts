---
platform: linux

inputs:
- name: accounts-git

outputs:
- name: accounts-ami

params:
  AWS_SECRET: ((aws-sandbox-secret-access-key))
  AWS_KEY: ((aws-sandbox-secret-key-id))
  
run:
  path: accounts-git/concourse/build-ami/build-ami.sh
