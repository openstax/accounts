<% @hide_session_info = true %>

<% page_heading 'Create Account' %>

<%# TODO move shared/new_account into views/signup? %>

<%= render partial: 'shared/new_account', locals: { url: '/auth/identity/register' } %>
