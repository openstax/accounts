<%
  login_state = get_login_state
  username_or_email = login_state[:username_or_email]
%>

<%= ox_card(classes: "login", heading: (t :'.name_greeting', name: username_or_email)) do %>

  <%= render partial: 'authenticate_options', locals: {source: 'authenticate'} %>

<% end %>
