<%
    current_step = I18n.t(
        :"login_signup_form.step_counter",
        current_step: 2,
        total_steps: 2
    )
%>

<%=
    render(
        partial: 'newflow/signup/change_signup_email_form_template',
        locals: {
            submit_to_path: student_change_signup_email_path,
            current_step: current_step,
            email_address_value: @email
        }
    )
%>
