<% # Clients of this partial must supply the following variables:
   # user
%>

<%= form_for user, url: user_path, html: {class: 'form-horizontal'} do |f| %>
  <div class="row">
    <span class="col-sm-1">
      <%= f.label :title, "Title" %>
    </span>

    <span class="col-sm-4">
      <%= f.label :first_name, "First Name" %>
    </span>

    <span class="col-sm-4">
      <%= f.label :last_name, "Last Name" %>
    </span>
  </div>
  <div class="row">
    <span class="col-sm-1">
      <%= f.text_field :title %>
    </span>

    <span class="col-sm-4">
      <%= f.text_field :first_name, value: user.first_name || \
                                           user.guessed_first_name %>
    </span>

    <span class="col-sm-4">
      <%= f.text_field :last_name, value: user.last_name || \
                                          user.guessed_last_name %>
    </span>

    <span class="col-sm-3">
      <%= f.submit "Update Profile", class: 'standard' %>
    </span>
  </div>
<% end %>
