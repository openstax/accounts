<%#= page_heading "Haven't seen you before &mdash; are you new to OpenStax?".html_safe %>

<% authentications = current_user.authentications %>

<%= page_heading "Great, you are signed in, but we #{'(still)' if authentications.size > 1} haven't seen you before.".html_safe %>

<div class='new-or-returning-buttons'>
  <%= link_to "I think I already have an account".html_safe, 
              login_path, class: 'btn btn-primary', style: 'margin-bottom: 28px; width: 270px' %>

  <div class="or-bar">
    <div class="or-text">OR</div>
  </div>


  <%= link_to "I am a brand new user".html_safe, 
              sessions_return_to_app_path, class: 'btn btn-primary', style: 'width: 270px' %>              
</div>

<div style="width: 500px; padding-top:10px">

  <p>So far the account you're currently signed in to is linked to <%= authentications_list(authentications) %>.</p>

  <p>If you think you've created an account before using <%= remaining_authentications_list(authentications) %>, click the top button to find that account.</p>

  <p>Otherwise click the bottom button to finish creating your new account.</p>

</div>
