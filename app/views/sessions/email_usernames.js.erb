<%
  message = t("sessions.start.sent_multiple_usernames",
              email: get_login_state[:username_or_email]).html_safe
%>

$('.alert').html('<%= message %>');
$('#login_username_or_email').val('');
$('#login_username_or_email').attr('placeholder',
                                   '<%= t("sessions.start.username_placeholder") %>');
